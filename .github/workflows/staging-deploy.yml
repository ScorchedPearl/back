name: Deploy to Staging

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Docker image to Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/pearlpostbackend:{{ github.sha }}

      # - name: Set up SSH key
      #   uses: webfactory/ssh-agent@v0.9.0
      #   with:
      #     ssh-private-key: ${{ secrets.VM_SSH_KEY }}

      # - name: Deploy on VM via SSH
      #   run: |
      #     ssh -o StrictHostKeyChecking=no ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} '
      #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/pearlpostbackend:staging && \
      #       docker stop pearlpostbackend || true && \
      #       docker rm pearlpostbackend || true && \
      #       docker run -d --name pearlpostbackend -p 8000:8000 -p 8080:8080 --env NODE_ENV=production --env DATABASE_URL=${{ secrets.DATABASE_URL }} ${{ secrets.DOCKERHUB_USERNAME }}/pearlpostbackend:staging
      #     '
